import SavedAgentsListingsWrapper from 'components/saved-agents-listings/SavedAgentsListingsWrapper';
import React from 'react';

/**
 * Saved Agents Listings component.
 *
 * @return {JSX.Element}
 */
function SavedAgentsListings() {
  return <SavedAgentsListingsWrapper />;
}

export default SavedAgentsListings;
